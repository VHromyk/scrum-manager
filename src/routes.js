const routes = {
  signup: '/signup',
  login: '/login',
  projects: '/projects',
  projectDetails: '/projects/:projectId',
  sprintDetails: '/projects/:projectId/sprints/:sprintId',
};

export default routes;
